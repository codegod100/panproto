{
  "lexicon": 1,
  "id": "dev.panproto.node.getHead",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["repo"],
        "properties": {
          "repo": { "type": "string", "maxLength": 256 }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["kind", "value"],
          "properties": {
            "kind": {
              "type": "string",
              "knownValues": ["branch", "detached"]
            },
            "value": { "type": "string", "maxLength": 256 }
          }
        }
      }
    }
  }
}
